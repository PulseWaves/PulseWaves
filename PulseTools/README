The first three PULSEtools pulseinfo.exe, pulseview.exe, and pulse2pulse.exe can parse various full waveform LiDAR formars. For the moment only the "pulse" component of each pulse (not the actual samples) are parsed.